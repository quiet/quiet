<?xml version='1.0' encoding='utf-8'?>
<category id="options" name="options" xmlns="http://jessevdk.github.com/cldoc/1.0">
  <struct name="quiet_dc_filter_options" ref="options::quiet_dc_filter_options#options::quiet_dc_filter_options">
    <brief> DC-blocking filter options</brief>
  </struct>
  <struct name="quiet_decoder_options" ref="options::quiet_decoder_options#options::quiet_decoder_options">
    <brief> Decoder options</brief>
  </struct>
  <struct name="quiet_demodulator_options" ref="options::quiet_demodulator_options#options::quiet_demodulator_options">
    <brief> Demodulator options</brief>
  </struct>
  <struct name="quiet_encoder_options" ref="options::quiet_encoder_options#options::quiet_encoder_options">
    <brief> Encoder options</brief>
  </struct>
  <struct name="quiet_modulator_options" ref="options::quiet_modulator_options#options::quiet_modulator_options">
    <brief> Modulator options</brief>
  </struct>
  <struct name="quiet_ofdm_options" ref="options::quiet_ofdm_options#options::quiet_ofdm_options">
    <brief> Encoder options for OFDM</brief>
  </struct>
  <struct name="quiet_resampler_options" ref="options::quiet_resampler_options#options::quiet_resampler_options">
    <brief> Resampler options</brief>
  </struct>
  <enum anonymous="yes" id="options::encodings" name="encodings">
    <enumvalue id="options::ofdm_encoding" name="ofdm_encoding" value="0">
      <brief>Encode/decode in OFDM mode</brief>
    </enumvalue>
    <enumvalue id="options::modem_encoding" name="modem_encoding" value="1">
      <brief>Encode/decode in modem mode</brief>
    </enumvalue>
    <enumvalue id="options::gmsk_encoding" name="gmsk_encoding" value="2">
      <doc>
Encode/decode in gaussian minimum shift keying mode

GMSK mode does not offer the modulation modes given by the other
encodings. It has a fairly limited bitrate, but the advantage of
GMSK is that its receiver does not need to compute any FFTs, making
it suitable for low-power receivers or situations with little
computational capacity.
</doc>
    </enumvalue>
  </enum>
  <typedef id="options::encoding_t" name="encoding_t">
    <brief> Encoder mode</brief>
    <doc>Selects operational mode for encoder/decoder. OFDM and Modem mode
use the same modulation schemes while gmsk ignores the supplied
scheme and uses its own
</doc>
    <type name="encodings" ref="options#options::encodings" />
  </typedef>
</category>
